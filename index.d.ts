/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export interface HttpsGithubComTwiNGatusV5ConfigConfig {
  [k: string]: unknown;
}
/**
 * Alert is endpoint.Endpoint's alert configuration
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Alert".
 */
export interface Alert {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * AlertProvider is the configuration necessary for sending an alert using AWS Simple Email Service
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "AlertProvider".
 */
export interface AlertProvider {
  'access-key-id': string;
  'secret-access-key': string;
  region: string;
  from: string;
  to: string;
  'default-alert'?: Alert1;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: Override[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert1 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Override".
 */
export interface Override {
  group: string;
  'access-key-id': string;
  'secret-access-key': string;
  region: string;
  from: string;
  to: string;
}
/**
 * Config is the configuration for alerting providers
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "AlertingConfig".
 */
export interface AlertingConfig {
  'aws-ses'?: AlertProvider1;
  clickup?: ClickupAlertProvider;
  custom?: CustomAlertProvider;
  datadog?: DatadogAlertProvider;
  discord?: DiscordAlertProvider;
  email?: EmailAlertProvider;
  github?: GithubAlertProvider;
  gitlab?: GitlabAlertProvider;
  gitea?: GiteaAlertProvider;
  googlechat?: GooglechatAlertProvider;
  gotify?: GotifyAlertProvider;
  homeassistant?: HomeassistantAlertProvider;
  ifttt?: IftttAlertProvider;
  ilert?: IlertAlertProvider;
  'incident-io'?: IncidentioAlertProvider;
  line?: LineAlertProvider;
  matrix?: MatrixAlertProvider;
  mattermost?: MattermostAlertProvider;
  messagebird?: MessagebirdAlertProvider;
  newrelic?: NewrelicAlertProvider;
  n8n?: N8NAlertProvider;
  ntfy?: NtfyAlertProvider;
  opsgenie?: OpsgenieAlertProvider;
  pagerduty?: PagerdutyAlertProvider;
  plivo?: PlivoAlertProvider;
  pushover?: PushoverAlertProvider;
  rocketchat?: RocketchatAlertProvider;
  sendgrid?: SendgridAlertProvider;
  signal?: SignalAlertProvider;
  signl4?: Signl4AlertProvider;
  slack?: SlackAlertProvider;
  splunk?: SplunkAlertProvider;
  squadcast?: SquadcastAlertProvider;
  teams?: TeamsAlertProvider;
  'teams-workflows'?: TeamsworkflowsAlertProvider;
  telegram?: TelegramAlertProvider;
  twilio?: TwilioAlertProvider;
  vonage?: VonageAlertProvider;
  webex?: WebexAlertProvider;
  zapier?: ZapierAlertProvider;
  zulip?: ZulipAlertProvider;
}
/**
 * AWSSimpleEmailService is the configuration for the aws-ses alerting provider
 */
export interface AlertProvider1 {
  'access-key-id': string;
  'secret-access-key': string;
  region: string;
  from: string;
  to: string;
  'default-alert'?: Alert1;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: Override[];
}
/**
 * ClickUp is the configuration for the clickup alerting provider
 */
export interface ClickupAlertProvider {
  'api-url': string;
  'list-id': string;
  token: string;
  assignees: string[];
  status: string;
  priority: string;
  'notify-all'?: boolean;
  name?: string;
  content?: string;
  'default-alert'?: Alert2;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: ClickupOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert2 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default configuration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ClickupOverride".
 */
export interface ClickupOverride {
  group: string;
  'api-url': string;
  'list-id': string;
  token: string;
  assignees: string[];
  status: string;
  priority: string;
  'notify-all'?: boolean;
  name?: string;
  content?: string;
}
/**
 * Custom is the configuration for the custom alerting provider
 */
export interface CustomAlertProvider {
  url: string;
  method?: string;
  body?: string;
  headers?: {
    [k: string]: string;
  };
  placeholders?: {
    [k: string]: {
      [k: string]: string;
    };
  };
  client?: ClientConfig;
  'default-alert'?: Alert3;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: CustomOverride[];
}
/**
 * ClientConfig is the configuration of the client used to communicate with the provider's target
 */
export interface ClientConfig {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * OAuth2Config is the OAuth2 configuration used for the client.
 *
 * If non-nil, the http.Client returned by getHTTPClient will automatically retrieve a token if necessary.
 * See configureOAuth2 for more details.
 */
export interface OAuth2Config {
  /**
   * e.g. https://dev-12345678.okta.com/token
   */
  'token-url': string;
  'client-id': string;
  'client-secret': string;
  /**
   * e.g. ["openid"]
   */
  scopes: string[];
}
/**
 * IAPConfig is the Google Cloud Identity-Aware-Proxy configuration used for the client. (e.g. audience)
 */
export interface IAPConfig {
  /**
   * e.g. "toto.apps.googleusercontent.com"
   */
  audience: string;
}
/**
 * TLS configuration (optional)
 */
export interface TLSConfig {
  /**
   * CertificateFile is the public certificate for TLS in PEM format.
   */
  'certificate-file'?: string;
  /**
   * PrivateKeyFile is the private key file for TLS in PEM format.
   */
  'private-key-file'?: string;
  renegotiation?: string;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert3 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "CustomOverride".
 */
export interface CustomOverride {
  group: string;
  url: string;
  method?: string;
  body?: string;
  headers?: {
    [k: string]: string;
  };
  placeholders?: {
    [k: string]: {
      [k: string]: string;
    };
  };
  client?: ClientConfig1;
}
/**
 * ClientConfig is the configuration of the client used to communicate with the provider's target
 */
export interface ClientConfig1 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * Datadog is the configuration for the datadog alerting provider
 */
export interface DatadogAlertProvider {
  /**
   * Datadog API key
   */
  'api-key': string;
  /**
   * Datadog site (e.g., datadoghq.com, datadoghq.eu)
   */
  site?: string;
  /**
   * Additional tags to include
   */
  tags?: string[];
  'default-alert'?: Alert4;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: DatadogOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert4 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "DatadogOverride".
 */
export interface DatadogOverride {
  group: string;
  /**
   * Datadog API key
   */
  'api-key': string;
  /**
   * Datadog site (e.g., datadoghq.com, datadoghq.eu)
   */
  site?: string;
  /**
   * Additional tags to include
   */
  tags?: string[];
}
/**
 * Discord is the configuration for the discord alerting provider
 */
export interface DiscordAlertProvider {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  /**
   * Message content for pinging users or groups (e.g. "<@123456789>" or "<@&987654321>")
   */
  'message-content'?: string;
  'default-alert'?: Alert5;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: DiscordOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert5 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "DiscordOverride".
 */
export interface DiscordOverride {
  group: string;
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  /**
   * Message content for pinging users or groups (e.g. "<@123456789>" or "<@&987654321>")
   */
  'message-content'?: string;
}
/**
 * Email is the configuration for the email alerting provider
 */
export interface EmailAlertProvider {
  from: string;
  username: string;
  password: string;
  host: string;
  port: number;
  to: string;
  client?: ClientConfig2;
  'default-alert'?: Alert6;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: EmailOverride[];
}
/**
 * ClientConfig is the configuration of the client used to communicate with the provider's target
 */
export interface ClientConfig2 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert6 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "EmailOverride".
 */
export interface EmailOverride {
  group: string;
  from: string;
  username: string;
  password: string;
  host: string;
  port: number;
  to: string;
  client?: ClientConfig3;
}
/**
 * ClientConfig is the configuration of the client used to communicate with the provider's target
 */
export interface ClientConfig3 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * GitHub is the configuration for the github alerting provider
 */
export interface GithubAlertProvider {
  /**
   * The URL of the GitHub repository to create issues in
   */
  'repository-url': string;
  /**
   * Token requires at least RW on issues and RO on metadata
   */
  token: string;
  'default-alert'?: Alert7;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert7 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * GitLab is the configuration for the gitlab alerting provider
 */
export interface GitlabAlertProvider {
  /**
   * The webhook url provided by GitLab
   */
  'webhook-url': string;
  /**
   * The authorization key provided by GitLab
   */
  'authorization-key': string;
  /**
   * Severity can be one of: critical, high, medium, low, info, unknown. Defaults to critical
   */
  severity?: string;
  /**
   * MonitoringTool overrides the name sent to gitlab. Defaults to gatus
   */
  'monitoring-tool'?: string;
  /**
   * EnvironmentName is the name of the associated GitLab environment. Required to display alerts on a dashboard.
   */
  'environment-name'?: string;
  /**
   * Service affected. Defaults to the endpoint's display name
   */
  service?: string;
  'default-alert'?: Alert8;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert8 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Gitea is the configuration for the gitea alerting provider
 */
export interface GiteaAlertProvider {
  /**
   * The URL of the Gitea repository to create issues in
   */
  'repository-url': string;
  /**
   * Token requires at least RW on issues and RO on metadata
   */
  token: string;
  /**
   * Assignees is a list of users to assign the issue to
   */
  assignees?: string[];
  client?: ClientConfig4;
  'default-alert'?: Alert9;
}
/**
 * ClientConfig is the configuration of the client used to communicate with the provider's target
 */
export interface ClientConfig4 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert9 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * GoogleChat is the configuration for the googlechat alerting provider
 */
export interface GooglechatAlertProvider {
  'webhook-url': string;
  client?: ClientConfig5;
  'default-alert'?: Alert10;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: GooglechatOverride[];
}
/**
 * Config is the configuration for clients
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ClientConfig".
 */
export interface ClientConfig5 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert10 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "GooglechatOverride".
 */
export interface GooglechatOverride {
  group: string;
  'webhook-url': string;
  client?: ClientConfig5;
}
/**
 * Gotify is the configuration for the gotify alerting provider
 */
export interface GotifyAlertProvider {
  /**
   * URL of the Gotify server
   */
  'server-url': string;
  /**
   * Token to use when sending a message to the Gotify server
   */
  token: string;
  /**
   * Priority of the message. Defaults to DefaultPriority.
   */
  priority?: number;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert11;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert11 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * HomeAssistant is the configuration for the homeassistant alerting provider
 */
export interface HomeassistantAlertProvider {
  url: string;
  token: string;
  'default-alert'?: Alert12;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: HomeassistantOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert12 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "HomeassistantOverride".
 */
export interface HomeassistantOverride {
  group: string;
  url: string;
  token: string;
}
/**
 * IFTTT is the configuration for the ifttt alerting provider
 */
export interface IftttAlertProvider {
  /**
   * IFTTT Webhook key
   */
  'webhook-key': string;
  /**
   * IFTTT event name
   */
  'event-name': string;
  'default-alert'?: Alert13;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: IftttOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert13 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "IftttOverride".
 */
export interface IftttOverride {
  group: string;
  /**
   * IFTTT Webhook key
   */
  'webhook-key': string;
  /**
   * IFTTT event name
   */
  'event-name': string;
}
/**
 * Ilert is the configuration for the ilert alerting provider
 */
export interface IlertAlertProvider {
  'integration-key': string;
  'default-alert'?: Alert14;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: IlertOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert14 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "IlertOverride".
 */
export interface IlertOverride {
  group: string;
  'integration-key': string;
}
/**
 * IncidentIO is the configuration for the incident-io alerting provider
 */
export interface IncidentioAlertProvider {
  url?: string;
  'auth-token'?: string;
  'source-url'?: string;
  metadata?: {
    [k: string]: unknown;
  };
  'default-alert'?: Alert15;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: IncidentioOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert15 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "IncidentioOverride".
 */
export interface IncidentioOverride {
  group: string;
  url?: string;
  'auth-token'?: string;
  'source-url'?: string;
  metadata?: {
    [k: string]: unknown;
  };
}
/**
 * Line is the configuration for the line alerting provider
 */
export interface LineAlertProvider {
  /**
   * Line Messaging API channel access token
   */
  'channel-access-token': string;
  /**
   * List of Line user IDs to send messages to
   */
  'user-ids': string[];
  'default-alert'?: Alert16;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: LineOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert16 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "LineOverride".
 */
export interface LineOverride {
  group: string;
  /**
   * Line Messaging API channel access token
   */
  'channel-access-token': string;
  /**
   * List of Line user IDs to send messages to
   */
  'user-ids': string[];
}
/**
 * Matrix is the configuration for the matrix alerting provider
 */
export interface MatrixAlertProvider {
  /**
   * ServerURL is the custom homeserver to use (optional)
   */
  'server-url': string;
  /**
   * AccessToken is the bot user's access token to send messages
   */
  'access-token': string;
  /**
   * InternalRoomID is the room that the bot user has permissions to send messages to
   */
  'internal-room-id': string;
  'default-alert'?: Alert17;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: MatrixOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert17 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "MatrixOverride".
 */
export interface MatrixOverride {
  group: string;
  /**
   * ServerURL is the custom homeserver to use (optional)
   */
  'server-url': string;
  /**
   * AccessToken is the bot user's access token to send messages
   */
  'access-token': string;
  /**
   * InternalRoomID is the room that the bot user has permissions to send messages to
   */
  'internal-room-id': string;
}
/**
 * Mattermost is the configuration for the mattermost alerting provider
 */
export interface MattermostAlertProvider {
  'webhook-url': string;
  channel?: string;
  client?: ClientConfig5;
  'default-alert'?: Alert18;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: MattermostOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert18 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "MattermostOverride".
 */
export interface MattermostOverride {
  group: string;
  'webhook-url': string;
  channel?: string;
  client?: ClientConfig5;
}
/**
 * Messagebird is the configuration for the messagebird alerting provider
 */
export interface MessagebirdAlertProvider {
  'access-key': string;
  originator: string;
  recipients: string;
  'default-alert'?: Alert19;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert19 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * NewRelic is the configuration for the newrelic alerting provider
 */
export interface NewrelicAlertProvider {
  /**
   * New Relic Insert key
   */
  'insert-key': string;
  /**
   * New Relic account ID
   */
  'account-id': string;
  /**
   * Region (US or EU, defaults to US)
   */
  region?: string;
  'default-alert'?: Alert20;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: NewrelicOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert20 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "NewrelicOverride".
 */
export interface NewrelicOverride {
  group: string;
  /**
   * New Relic Insert key
   */
  'insert-key': string;
  /**
   * New Relic account ID
   */
  'account-id': string;
  /**
   * Region (US or EU, defaults to US)
   */
  region?: string;
}
/**
 * N8N is the configuration for the n8n alerting provider
 */
export interface N8NAlertProvider {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert21;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: N8NOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert21 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "N8nOverride".
 */
export interface N8NOverride {
  group: string;
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
}
/**
 * Ntfy is the configuration for the ntfy alerting provider
 */
export interface NtfyAlertProvider {
  topic: string;
  /**
   * Defaults to DefaultURL
   */
  url?: string;
  /**
   * Defaults to DefaultPriority
   */
  priority?: number;
  /**
   * Defaults to ""
   */
  token?: string;
  /**
   * Defaults to ""
   */
  email?: string;
  /**
   * Defaults to ""
   */
  click?: string;
  /**
   * Defaults to false
   */
  'disable-firebase'?: boolean;
  /**
   * Defaults to false
   */
  'disable-cache'?: boolean;
  'default-alert'?: Alert22;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: NtfyOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert22 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "NtfyOverride".
 */
export interface NtfyOverride {
  group: string;
  topic: string;
  /**
   * Defaults to DefaultURL
   */
  url?: string;
  /**
   * Defaults to DefaultPriority
   */
  priority?: number;
  /**
   * Defaults to ""
   */
  token?: string;
  /**
   * Defaults to ""
   */
  email?: string;
  /**
   * Defaults to ""
   */
  click?: string;
  /**
   * Defaults to false
   */
  'disable-firebase'?: boolean;
  /**
   * Defaults to false
   */
  'disable-cache'?: boolean;
}
/**
 * Opsgenie is the configuration for the opsgenie alerting provider
 */
export interface OpsgenieAlertProvider {
  /**
   * APIKey to use for
   */
  'api-key': string;
  /**
   * Priority to be used in Opsgenie alert payload
   *
   * default: P1
   */
  priority: string;
  /**
   * Source define source to be used in Opsgenie alert payload
   *
   * default: gatus
   */
  source: string;
  /**
   * EntityPrefix is a prefix to be used in entity argument in Opsgenie alert payload
   *
   * default: gatus-
   */
  'entity-prefix': string;
  /**
   * AliasPrefix is a prefix to be used in alias argument in Opsgenie alert payload
   *
   * default: gatus-healthcheck-
   */
  'alias-prefix': string;
  /**
   * Tags to be used in Opsgenie alert payload
   *
   * default: []
   */
  tags: string[];
  'default-alert'?: Alert23;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert23 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * PagerDuty is the configuration for the pagerduty alerting provider
 */
export interface PagerdutyAlertProvider {
  'integration-key': string;
  'default-alert'?: Alert24;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: PagerdutyOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert24 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "PagerdutyOverride".
 */
export interface PagerdutyOverride {
  group: string;
  'integration-key': string;
}
/**
 * Plivo is the configuration for the plivo alerting provider
 */
export interface PlivoAlertProvider {
  'auth-id': string;
  'auth-token': string;
  from: string;
  to: string[];
  'default-alert'?: Alert25;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: PlivoOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert25 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "PlivoOverride".
 */
export interface PlivoOverride {
  group: string;
  'auth-id': string;
  'auth-token': string;
  from: string;
  to: string[];
}
/**
 * Pushover is the configuration for the pushover alerting provider
 */
export interface PushoverAlertProvider {
  /**
   * Key used to authenticate the application sending
   * See "Your Applications" on the dashboard, or add a new one: https://pushover.net/apps/build
   */
  'application-token': string;
  /**
   * Key of the user or group the messages should be sent to
   */
  'user-key': string;
  /**
   * The title of your message
   * default: "Gatus: <endpoint>""
   */
  title?: string;
  /**
   * Priority of all messages, ranging from -2 (very low) to 2 (Emergency)
   * default: 0
   */
  priority?: number;
  /**
   * Priority of resolved messages, ranging from -2 (very low) to 2 (Emergency)
   * default: 0
   */
  'resolved-priority'?: number;
  /**
   * Sound of the messages (see: https://pushover.net/api#sounds)
   * default: "" (pushover)
   */
  sound?: string;
  /**
   * TTL of your message (https://pushover.net/api#ttl)
   * If priority is 2 then this parameter is ignored
   * default: 0
   */
  ttl?: number;
  /**
   * Device to send the message to (see: https://pushover.net/api#devices)
   * default: "" (all devices)
   */
  device?: string;
  'default-alert'?: Alert26;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert26 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * RocketChat is the configuration for the rocketchat alerting provider
 */
export interface RocketchatAlertProvider {
  /**
   * Rocket.Chat incoming webhook URL
   */
  'webhook-url': string;
  /**
   * Optional channel override
   */
  channel?: string;
  'default-alert'?: Alert27;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: RocketchatOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert27 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "RocketchatOverride".
 */
export interface RocketchatOverride {
  group: string;
  /**
   * Rocket.Chat incoming webhook URL
   */
  'webhook-url': string;
  /**
   * Optional channel override
   */
  channel?: string;
}
/**
 * SendGrid is the configuration for the sendgrid alerting provider
 */
export interface SendgridAlertProvider {
  'api-key': string;
  from: string;
  to: string;
  client?: ClientConfig6;
  'default-alert'?: Alert28;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SendgridOverride[];
}
/**
 * Config is the configuration for clients
 */
export interface ClientConfig6 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert28 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SendgridOverride".
 */
export interface SendgridOverride {
  group: string;
  'api-key': string;
  from: string;
  to: string;
  client?: ClientConfig7;
}
/**
 * Config is the configuration for clients
 */
export interface ClientConfig7 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * Signal is the configuration for the signal alerting provider
 */
export interface SignalAlertProvider {
  /**
   * Signal API URL (e.g., signal-cli-rest-api instance)
   */
  'api-url': string;
  /**
   * Sender phone number
   */
  number: string;
  /**
   * List of recipient phone numbers
   */
  recipients: string[];
  'default-alert'?: Alert29;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SignalOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert29 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SignalOverride".
 */
export interface SignalOverride {
  group: string;
  /**
   * Signal API URL (e.g., signal-cli-rest-api instance)
   */
  'api-url': string;
  /**
   * Sender phone number
   */
  number: string;
  /**
   * List of recipient phone numbers
   */
  recipients: string[];
}
/**
 * SIGNL4 is the configuration for the signl4 alerting provider
 */
export interface Signl4AlertProvider {
  /**
   * SIGNL4 team secret
   */
  'team-secret': string;
  'default-alert'?: Alert30;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: Signl4Override[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert30 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Signl4Override".
 */
export interface Signl4Override {
  group: string;
  /**
   * SIGNL4 team secret
   */
  'team-secret': string;
}
/**
 * Slack is the configuration for the slack alerting provider
 */
export interface SlackAlertProvider {
  /**
   * Slack webhook URL
   */
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert31;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SlackOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert31 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SlackOverride".
 */
export interface SlackOverride {
  group: string;
  /**
   * Slack webhook URL
   */
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
}
/**
 * Splunk is the configuration for the splunk alerting provider
 */
export interface SplunkAlertProvider {
  /**
   * Splunk HEC (HTTP Event Collector) URL
   */
  'hec-url': string;
  /**
   * Splunk HEC token
   */
  'hec-token': string;
  /**
   * Event source
   */
  source?: string;
  /**
   * Event source type
   */
  sourcetype?: string;
  /**
   * Splunk index
   */
  index?: string;
  'default-alert'?: Alert32;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SplunkOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert32 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SplunkOverride".
 */
export interface SplunkOverride {
  group: string;
  /**
   * Splunk HEC (HTTP Event Collector) URL
   */
  'hec-url': string;
  /**
   * Splunk HEC token
   */
  'hec-token': string;
  /**
   * Event source
   */
  source?: string;
  /**
   * Event source type
   */
  sourcetype?: string;
  /**
   * Splunk index
   */
  index?: string;
}
/**
 * Squadcast is the configuration for the squadcast alerting provider
 */
export interface SquadcastAlertProvider {
  /**
   * Squadcast webhook URL
   */
  'webhook-url': string;
  'default-alert'?: Alert33;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SquadcastOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert33 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SquadcastOverride".
 */
export interface SquadcastOverride {
  group: string;
  /**
   * Squadcast webhook URL
   */
  'webhook-url': string;
}
/**
 * Teams is the configuration for the teams alerting provider
 */
export interface TeamsAlertProvider {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  client?: ClientConfig8;
  'default-alert'?: Alert34;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: TeamsOverride[];
}
/**
 * Config is the configuration for clients
 */
export interface ClientConfig8 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert34 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TeamsOverride".
 */
export interface TeamsOverride {
  group: string;
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  client?: ClientConfig9;
}
/**
 * Config is the configuration for clients
 */
export interface ClientConfig9 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * TeamsWorkflows is the configuration for the teams alerting provider using the new Workflow App Webhook Connector
 */
export interface TeamsworkflowsAlertProvider {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert35;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: TeamsworkflowsOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert35 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TeamsworkflowsOverride".
 */
export interface TeamsworkflowsOverride {
  group: string;
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
}
/**
 * Telegram is the configuration for the telegram alerting provider
 */
export interface TelegramAlertProvider {
  token: string;
  id: string;
  'topic-id'?: string;
  'api-url': string;
  client?: ClientConfig5;
  'default-alert'?: Alert36;
  /**
   * Overrides is a list of overrides that may be prioritized over the default configuration
   */
  overrides?: TelegramOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert36 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a configuration that may be prioritized over the default configuration
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TelegramOverride".
 */
export interface TelegramOverride {
  group: string;
  token: string;
  id: string;
  'topic-id'?: string;
  'api-url': string;
  client?: ClientConfig5;
}
/**
 * Twilio is the configuration for the twilio alerting provider
 */
export interface TwilioAlertProvider {
  sid: string;
  token: string;
  from: string;
  to: string;
  /**
   * TODO in v6.0.0: Rename this to text-triggered
   */
  'text-twilio-triggered'?: string;
  /**
   * TODO in v6.0.0: Rename this to text-resolved
   */
  'text-twilio-resolved'?: string;
  'default-alert'?: Alert37;
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert37 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Vonage is the configuration for the vonage alerting provider
 */
export interface VonageAlertProvider {
  'api-key': string;
  'api-secret': string;
  from: string;
  to: string[];
  'default-alert'?: Alert38;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: VonageOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert38 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "VonageOverride".
 */
export interface VonageOverride {
  group: string;
  'api-key': string;
  'api-secret': string;
  from: string;
  to: string[];
}
/**
 * Webex is the configuration for the webex alerting provider
 */
export interface WebexAlertProvider {
  /**
   * Webex Teams webhook URL
   */
  'webhook-url': string;
  'default-alert'?: Alert39;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: WebexOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert39 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "WebexOverride".
 */
export interface WebexOverride {
  group: string;
  /**
   * Webex Teams webhook URL
   */
  'webhook-url': string;
}
/**
 * Zapier is the configuration for the zapier alerting provider
 */
export interface ZapierAlertProvider {
  /**
   * Zapier webhook URL
   */
  'webhook-url': string;
  'default-alert'?: Alert40;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: ZapierOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert40 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ZapierOverride".
 */
export interface ZapierOverride {
  group: string;
  /**
   * Zapier webhook URL
   */
  'webhook-url': string;
}
/**
 * Zulip is the configuration for the zulip alerting provider
 */
export interface ZulipAlertProvider {
  /**
   * Email of the bot user
   */
  'bot-email': string;
  /**
   * API key of the bot user
   */
  'bot-api-key': string;
  /**
   * Domain of the Zulip server
   */
  domain: string;
  /**
   * ID of the channel to send the message to
   */
  'channel-id': string;
  'default-alert'?: Alert41;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: ZulipOverride[];
}
/**
 * DefaultAlert is the default alert configuration to use for endpoints with an alert of the appropriate type
 */
export interface Alert41 {
  /**
   * Type of alert (required)
   */
  type: string;
  /**
   * Enabled defines whether the alert is enabled
   *
   * Use Alert.IsEnabled() to retrieve the value of this field.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  enabled?: boolean;
  /**
   * FailureThreshold is the number of failures in a row needed before triggering the alert
   */
  'failure-threshold': number;
  /**
   * SuccessThreshold defines how many successful executions must happen in a row before an ongoing incident is marked as resolved
   */
  'success-threshold': number;
  /**
   * MinimumReminderInterval is the interval between reminders
   */
  'minimum-reminder-interval'?: string;
  /**
   * Description of the alert. Will be included in the alert sent.
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work.
   */
  description?: string;
  /**
   * SendOnResolved defines whether to send a second notification when the issue has been resolved
   *
   * This is a pointer, because it is populated by YAML and we need to know whether it was explicitly set to a value
   * or not for provider.ParseWithDefaultAlert to work. Use Alert.IsSendingOnResolved() for a non-pointer
   */
  'send-on-resolved'?: boolean;
  /**
   * ProviderOverride is an optional field that can be used to override the provider's configuration
   * It is freeform so that it can be used for any provider-specific configuration.
   */
  'provider-override'?: {
    [k: string]: unknown;
  };
}
/**
 * Override is a case under which the default integration is overridden
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ZulipOverride".
 */
export interface ZulipOverride {
  group: string;
  /**
   * Email of the bot user
   */
  'bot-email': string;
  /**
   * API key of the bot user
   */
  'bot-api-key': string;
  /**
   * Domain of the Zulip server
   */
  domain: string;
  /**
   * ID of the channel to send the message to
   */
  'channel-id': string;
}
/**
 * Announcement represents a system-wide announcement
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Announcement".
 */
export interface Announcement {
  /**
   * Timestamp is the UTC timestamp when the announcement was made
   */
  timestamp: string;
  /**
   * Type is the type of announcement (outage, warning, information, operational, none)
   */
  type: string;
  /**
   * Message is the user-facing text describing the announcement
   */
  message: string;
  /**
   * Archived indicates whether the announcement should be displayed in the historical section
   * instead of at the top of the status page
   */
  archived?: boolean;
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Badge".
 */
export interface Badge {
  'response-time': ResponseTime;
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ResponseTime".
 */
export interface ResponseTime {
  thresholds: number[];
}
/**
 * BasicConfig is the configuration for Basic authentication
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "BasicConfig".
 */
export interface BasicConfig {
  /**
   * Username is the name which will need to be used for a successful authentication
   */
  username: string;
  /**
   * PasswordBcryptHashBase64Encoded is the base64 encoded string of the Bcrypt hash of the password to use to
   * authenticate using basic auth.
   */
  'password-bcrypt-base64': string;
}
/**
 * Checker is the configuration for making sure Gatus has access to the internet.
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Checker".
 */
export interface Checker {
  /**
   * e.g. 1.1.1.1:53
   */
  target: string;
  interval?: string;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using ClickUp
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ClickupAlertProvider".
 */
export interface ClickupAlertProvider1 {
  'api-url': string;
  'list-id': string;
  token: string;
  assignees: string[];
  status: string;
  priority: string;
  'notify-all'?: boolean;
  name?: string;
  content?: string;
  'default-alert'?: Alert2;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: ClickupOverride[];
}
/**
 * Config is the main configuration structure
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Config".
 */
export interface Config {
  /**
   * Debug Whether to enable debug logs
   * Deprecated: Use the GATUS_LOG_LEVEL environment variable instead
   */
  debug?: boolean;
  /**
   * Metrics Whether to expose metrics at /metrics
   */
  metrics?: boolean;
  /**
   * SkipInvalidConfigUpdate Whether to make the application ignore invalid configuration
   * if the configuration file is updated while the application is running
   */
  'skip-invalid-config-update'?: boolean;
  /**
   * DisableMonitoringLock Whether to disable the monitoring lock
   * The monitoring lock is what prevents multiple endpoints from being processed at the same time.
   * Disabling this may lead to inaccurate response times
   *
   * Deprecated: Use Concurrency instead TODO: REMOVE THIS IN v6.0.0
   */
  'disable-monitoring-lock'?: boolean;
  /**
   * Concurrency is the maximum number of endpoints/suites that can be monitored concurrently
   * Defaults to DefaultConcurrency. Set to 0 for unlimited concurrency.
   */
  concurrency?: number;
  security?: SecurityConfig;
  alerting?: AlertingConfig1;
  /**
   * Endpoints is the list of endpoints to monitor
   */
  endpoints?: Endpoint[];
  /**
   * ExternalEndpoints is the list of all external endpoints
   */
  'external-endpoints'?: ExternalEndpoint[];
  /**
   * Suites is the list of suites to monitor
   */
  suites?: Suite[];
  storage?: StorageConfig;
  web?: WebConfig;
  ui?: UiConfig1;
  maintenance?: MaintenanceConfig1;
  remote?: RemoteConfig;
  connectivity?: ConnectivityConfig;
  tunneling?: TunnelingConfig;
  /**
   * Announcements is the list of system-wide announcements
   */
  announcements?: Announcement[];
}
/**
 * Security is the configuration for securing access to Gatus
 */
export interface SecurityConfig {
  basic?: BasicConfig;
  oidc?: OIDCConfig;
}
/**
 * OIDCConfig is the configuration for OIDC authentication
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "OIDCConfig".
 */
export interface OIDCConfig {
  /**
   * e.g. https://dev-12345678.okta.com
   */
  'issuer-url': string;
  /**
   * e.g. http://localhost:8080/authorization-code/callback
   */
  'redirect-url': string;
  'client-id': string;
  'client-secret': string;
  /**
   * e.g. ["openid"]
   */
  scopes: string[];
  /**
   * e.g. ["user1@example.com"]. If empty, all subjects are allowed
   */
  'allowed-subjects': string[];
  /**
   * e.g. 8h. Defaults to 8 hours
   */
  'session-ttl': string;
}
/**
 * Alerting is the configuration for alerting providers
 */
export interface AlertingConfig1 {
  'aws-ses'?: AlertProvider1;
  clickup?: ClickupAlertProvider;
  custom?: CustomAlertProvider;
  datadog?: DatadogAlertProvider;
  discord?: DiscordAlertProvider;
  email?: EmailAlertProvider;
  github?: GithubAlertProvider;
  gitlab?: GitlabAlertProvider;
  gitea?: GiteaAlertProvider;
  googlechat?: GooglechatAlertProvider;
  gotify?: GotifyAlertProvider;
  homeassistant?: HomeassistantAlertProvider;
  ifttt?: IftttAlertProvider;
  ilert?: IlertAlertProvider;
  'incident-io'?: IncidentioAlertProvider;
  line?: LineAlertProvider;
  matrix?: MatrixAlertProvider;
  mattermost?: MattermostAlertProvider;
  messagebird?: MessagebirdAlertProvider;
  newrelic?: NewrelicAlertProvider;
  n8n?: N8NAlertProvider;
  ntfy?: NtfyAlertProvider;
  opsgenie?: OpsgenieAlertProvider;
  pagerduty?: PagerdutyAlertProvider;
  plivo?: PlivoAlertProvider;
  pushover?: PushoverAlertProvider;
  rocketchat?: RocketchatAlertProvider;
  sendgrid?: SendgridAlertProvider;
  signal?: SignalAlertProvider;
  signl4?: Signl4AlertProvider;
  slack?: SlackAlertProvider;
  splunk?: SplunkAlertProvider;
  squadcast?: SquadcastAlertProvider;
  teams?: TeamsAlertProvider;
  'teams-workflows'?: TeamsworkflowsAlertProvider;
  telegram?: TelegramAlertProvider;
  twilio?: TwilioAlertProvider;
  vonage?: VonageAlertProvider;
  webex?: WebexAlertProvider;
  zapier?: ZapierAlertProvider;
  zulip?: ZulipAlertProvider;
}
/**
 * Endpoint is the configuration of a service to be monitored
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Endpoint".
 */
export interface Endpoint {
  /**
   * Enabled defines whether to enable the monitoring of the endpoint
   */
  enabled?: boolean;
  /**
   * Name of the endpoint. Can be anything.
   */
  name: string;
  /**
   * Group the endpoint is a part of. Used for grouping multiple endpoints together on the front end.
   */
  group?: string;
  /**
   * URL to send the request to
   */
  url: string;
  /**
   * Method of the request made to the url of the endpoint
   */
  method?: string;
  /**
   * Body of the request
   */
  body?: string;
  /**
   * GraphQL is whether to wrap the body in a query param ({"query":"$body"})
   */
  graphql?: boolean;
  /**
   * Headers of the request
   */
  headers?: {
    [k: string]: string;
  };
  /**
   * ExtraLabels are key-value pairs that can be used to metric the endpoint
   */
  'extra-labels'?: {
    [k: string]: string;
  };
  /**
   * Interval is the duration to wait between every status check
   */
  interval?: string;
  /**
   * Conditions used to determine the health of the endpoint
   */
  conditions: string[];
  /**
   * Alerts is the alerting configuration for the endpoint in case of failure
   */
  alerts?: Alert[];
  /**
   * MaintenanceWindow is the configuration for per-endpoint maintenance windows
   */
  'maintenance-windows'?: MaintenanceConfig[];
  dns?: DnsConfig;
  ssh?: SshConfig;
  client?: ClientConfig10;
  ui?: UiConfig;
  /**
   * Store is a map of values to extract from the result and store in the suite context
   * This field is only used when the endpoint is part of a suite
   */
  store?: {
    [k: string]: string;
  };
  /**
   * AlwaysRun defines whether to execute this endpoint even if previous endpoints in the suite failed
   * This field is only used when the endpoint is part of a suite
   */
  'always-run'?: boolean;
}
/**
 * Config allows for the configuration of a maintenance period.
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "MaintenanceConfig".
 */
export interface MaintenanceConfig {
  /**
   * Whether the maintenance period is enabled. Enabled by default if nil.
   */
  enabled: boolean;
  /**
   * Time at which the maintenance period starts (e.g. 23:00)
   */
  start?: string;
  /**
   * Duration of the maintenance period (e.g. 4h)
   */
  duration?: string;
  /**
   * Timezone in string format which the maintenance period is configured (e.g. America/Sao_Paulo)
   */
  timezone?: string;
  /**
   * Every is a list of days of the week during which maintenance period applies.
   * See longDayNames for list of valid values.
   * Every day if empty.
   */
  every?: string[];
}
/**
 * DNSConfig is the configuration for DNS monitoring
 */
export interface DnsConfig {
  /**
   * QueryType is the type for the DNS records like A, AAAA, CNAME...
   */
  'query-type': string;
  /**
   * QueryName is the query for DNS
   */
  'query-name': string;
}
/**
 * SSH is the configuration for SSH monitoring
 */
export interface SshConfig {
  username?: string;
  password?: string;
  'private-key'?: string;
}
/**
 * Config is the configuration for clients
 */
export interface ClientConfig10 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * UIConfig is the configuration for the UI
 */
export interface UiConfig {
  /**
   * HideConditions whether to hide the condition results on the UI
   */
  'hide-conditions': boolean;
  /**
   * HideHostname whether to hide the hostname in the Result
   */
  'hide-hostname': boolean;
  /**
   * HideURL whether to ensure the URL is not displayed in the results. Useful if the URL contains a token.
   */
  'hide-url': boolean;
  /**
   * HidePort whether to hide the port in the Result
   */
  'hide-port': boolean;
  /**
   * HideErrors whether to hide the errors in the Result
   */
  'hide-errors': boolean;
  /**
   * DontResolveFailedConditions whether to resolve failed conditions in the Result for display in the UI
   */
  'dont-resolve-failed-conditions': boolean;
  /**
   * ResolveSuccessfulConditions whether to resolve successful conditions in the Result for display in the UI
   */
  'resolve-successful-conditions': boolean;
  badge: Badge1;
}
/**
 * Badge is the configuration for the badges generated
 */
export interface Badge1 {
  'response-time': ResponseTime;
}
/**
 * ExternalEndpoint is an endpoint whose result is pushed from outside Gatus, which means that said endpoints are not monitored by Gatus itself; Gatus only displays their results and takes care of alerting
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ExternalEndpoint".
 */
export interface ExternalEndpoint {
  /**
   * Enabled defines whether to enable the monitoring of the endpoint
   */
  enabled?: boolean;
  /**
   * Name of the endpoint. Can be anything.
   */
  name: string;
  /**
   * Group the endpoint is a part of. Used for grouping multiple endpoints together on the front end.
   */
  group?: string;
  /**
   * Token is the bearer token that must be provided through the Authorization header to push results to the endpoint
   */
  token?: string;
  /**
   * Alerts is the alerting configuration for the endpoint in case of failure
   */
  alerts?: Alert[];
  /**
   * MaintenanceWindow is the configuration for per-endpoint maintenance windows
   */
  'maintenance-windows'?: MaintenanceConfig[];
  heartbeat?: HeartbeatConfig;
}
/**
 * Heartbeat is the configuration that checks if the external endpoint has received new results when it should have.
 */
export interface HeartbeatConfig {
  /**
   * Interval is the time interval at which Gatus verifies whether the external endpoint has received new results
   * If no new result is received within the interval, the endpoint is marked as failed and alerts are triggered
   */
  interval: string;
}
/**
 * Suite is a collection of endpoints that are executed sequentially with shared context
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Suite".
 */
export interface Suite {
  /**
   * Name of the suite. Must be unique.
   */
  name: string;
  /**
   * Group the suite belongs to. Used for grouping multiple suites together.
   */
  group?: string;
  /**
   * Enabled defines whether the suite is enabled
   */
  enabled?: boolean;
  /**
   * Interval is the duration to wait between suite executions
   */
  interval?: string;
  /**
   * Timeout is the maximum duration for the entire suite execution
   */
  timeout?: string;
  /**
   * InitialContext holds initial values that can be referenced by endpoints
   */
  context?: {
    [k: string]: unknown;
  };
  /**
   * Endpoints in the suite (executed sequentially)
   */
  endpoints: Endpoint[];
}
/**
 * Storage is the configuration for how the data is stored
 */
export interface StorageConfig {
  /**
   * Path is the path used by the store to achieve persistence
   * If blank, persistence is disabled.
   * Note that not all Type support persistence
   */
  path: string;
  /**
   * Type of store
   * If blank, uses the default in-memory store
   */
  type: string;
  /**
   * Caching is whether to enable caching.
   * This is used to drastically decrease read latency by pre-emptively caching writes
   * as they happen, also known as the write-through caching strategy.
   * Does not apply if Config.Type is not TypePostgres or TypeSQLite.
   */
  caching?: boolean;
  /**
   * MaximumNumberOfResults is the number of results each endpoint should be able to provide
   */
  'maximum-number-of-results'?: number;
  /**
   * MaximumNumberOfEvents is the number of events each endpoint should be able to provide
   */
  'maximum-number-of-events'?: number;
}
/**
 * Web is the web configuration for the application
 */
export interface WebConfig {
  /**
   * Address to listen on (defaults to 0.0.0.0 specified by DefaultAddress)
   */
  address: string;
  /**
   * Port to listen on (default to 8080 specified by DefaultPort)
   */
  port: number;
  /**
   * ReadBufferSize sets fiber.Config.ReadBufferSize, which is the buffer size for reading requests coming from a
   * single connection and also acts as a limit for the maximum header size.
   *
   * If you're getting occasional "Request Header Fields Too Large", you may want to try increasing this value.
   *
   * Defaults to DefaultReadBufferSize
   */
  'read-buffer-size'?: number;
  tls?: WebTLSConfig;
}
/**
 * TLS configuration (optional)
 */
export interface WebTLSConfig {
  /**
   * CertificateFile is the public certificate for TLS in PEM format.
   */
  'certificate-file'?: string;
  /**
   * PrivateKeyFile is the private key file for TLS in PEM format.
   */
  'private-key-file'?: string;
}
/**
 * UI is the configuration for the UI
 */
export interface UiConfig1 {
  /**
   * HideConditions whether to hide the condition results on the UI
   */
  'hide-conditions': boolean;
  /**
   * HideHostname whether to hide the hostname in the Result
   */
  'hide-hostname': boolean;
  /**
   * HideURL whether to ensure the URL is not displayed in the results. Useful if the URL contains a token.
   */
  'hide-url': boolean;
  /**
   * HidePort whether to hide the port in the Result
   */
  'hide-port': boolean;
  /**
   * HideErrors whether to hide the errors in the Result
   */
  'hide-errors': boolean;
  /**
   * DontResolveFailedConditions whether to resolve failed conditions in the Result for display in the UI
   */
  'dont-resolve-failed-conditions': boolean;
  /**
   * ResolveSuccessfulConditions whether to resolve successful conditions in the Result for display in the UI
   */
  'resolve-successful-conditions': boolean;
  badge: Badge1;
}
/**
 * Config allows for the configuration of a maintenance period.
 */
export interface MaintenanceConfig1 {
  /**
   * Whether the maintenance period is enabled. Enabled by default if nil.
   */
  enabled: boolean;
  /**
   * Time at which the maintenance period starts (e.g. 23:00)
   */
  start?: string;
  /**
   * Duration of the maintenance period (e.g. 4h)
   */
  duration?: string;
  /**
   * Timezone in string format which the maintenance period is configured (e.g. America/Sao_Paulo)
   */
  timezone?: string;
  /**
   * Every is a list of days of the week during which maintenance period applies.
   * See longDayNames for list of valid values.
   * Every day if empty.
   */
  every?: string[];
}
/**
 * Remote is the configuration for remote Gatus instances
 * WARNING: This is in ALPHA and may change or be completely removed in the future
 */
export interface RemoteConfig {
  /**
   * Instances is a list of remote instances to retrieve endpoint statuses from.
   */
  instances?: Instance[];
  client?: ClientConfig11;
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Instance".
 */
export interface Instance {
  'endpoint-prefix': string;
  url: string;
}
/**
 * Config is the configuration for clients
 */
export interface ClientConfig11 {
  /**
   * ProxyURL is the URL of the proxy to use for the client
   */
  'proxy-url'?: string;
  /**
   * Insecure determines whether to skip verifying the server's certificate chain and host name
   */
  insecure?: boolean;
  /**
   * IgnoreRedirect determines whether to ignore redirects (true) or follow them (false, default)
   */
  'ignore-redirect'?: boolean;
  /**
   * Timeout for the client
   */
  timeout: string;
  /**
   * DNSResolver override for the HTTP client
   * Expected format is {protocol}://{host}:{port}, e.g. tcp://8.8.8.8:53
   */
  'dns-resolver'?: string;
  oauth2?: OAuth2Config;
  'identity-aware-proxy'?: IAPConfig;
  /**
   * Network (ip, ip4 or ip6) for the ICMP client
   */
  network: string;
  tls?: TLSConfig;
  /**
   * Tunnel is the name of the SSH tunnel to use for the client
   */
  tunnel?: string;
}
/**
 * Connectivity is the configuration for connectivity
 */
export interface ConnectivityConfig {
  checker?: Checker;
}
/**
 * Tunneling is the configuration for SSH tunneling
 */
export interface TunnelingConfig {}
/**
 * Config is the configuration for the connectivity checker.
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ConnectivityConfig".
 */
export interface ConnectivityConfig1 {
  checker?: Checker;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using a custom HTTP request Technically, all alert providers should be reachable using the custom alert provider
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "CustomAlertProvider".
 */
export interface CustomAlertProvider1 {
  url: string;
  method?: string;
  body?: string;
  headers?: {
    [k: string]: string;
  };
  placeholders?: {
    [k: string]: {
      [k: string]: string;
    };
  };
  client?: ClientConfig;
  'default-alert'?: Alert3;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: CustomOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Datadog
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "DatadogAlertProvider".
 */
export interface DatadogAlertProvider1 {
  /**
   * Datadog API key
   */
  'api-key': string;
  /**
   * Datadog site (e.g., datadoghq.com, datadoghq.eu)
   */
  site?: string;
  /**
   * Additional tags to include
   */
  tags?: string[];
  'default-alert'?: Alert4;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: DatadogOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Discord
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "DiscordAlertProvider".
 */
export interface DiscordAlertProvider1 {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  /**
   * Message content for pinging users or groups (e.g. "<@123456789>" or "<@&987654321>")
   */
  'message-content'?: string;
  'default-alert'?: Alert5;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: DiscordOverride[];
}
/**
 * Config for an Endpoint of type DNS
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "DnsConfig".
 */
export interface DnsConfig1 {
  /**
   * QueryType is the type for the DNS records like A, AAAA, CNAME...
   */
  'query-type': string;
  /**
   * QueryName is the query for DNS
   */
  'query-name': string;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using SMTP
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "EmailAlertProvider".
 */
export interface EmailAlertProvider1 {
  from: string;
  username: string;
  password: string;
  host: string;
  port: number;
  to: string;
  client?: ClientConfig2;
  'default-alert'?: Alert6;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: EmailOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Discord
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "GiteaAlertProvider".
 */
export interface GiteaAlertProvider1 {
  /**
   * The URL of the Gitea repository to create issues in
   */
  'repository-url': string;
  /**
   * Token requires at least RW on issues and RO on metadata
   */
  token: string;
  /**
   * Assignees is a list of users to assign the issue to
   */
  assignees?: string[];
  client?: ClientConfig4;
  'default-alert'?: Alert9;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Discord
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "GithubAlertProvider".
 */
export interface GithubAlertProvider1 {
  /**
   * The URL of the GitHub repository to create issues in
   */
  'repository-url': string;
  /**
   * Token requires at least RW on issues and RO on metadata
   */
  token: string;
  'default-alert'?: Alert7;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using GitLab
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "GitlabAlertProvider".
 */
export interface GitlabAlertProvider1 {
  /**
   * The webhook url provided by GitLab
   */
  'webhook-url': string;
  /**
   * The authorization key provided by GitLab
   */
  'authorization-key': string;
  /**
   * Severity can be one of: critical, high, medium, low, info, unknown. Defaults to critical
   */
  severity?: string;
  /**
   * MonitoringTool overrides the name sent to gitlab. Defaults to gatus
   */
  'monitoring-tool'?: string;
  /**
   * EnvironmentName is the name of the associated GitLab environment. Required to display alerts on a dashboard.
   */
  'environment-name'?: string;
  /**
   * Service affected. Defaults to the endpoint's display name
   */
  service?: string;
  'default-alert'?: Alert8;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Google chat
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "GooglechatAlertProvider".
 */
export interface GooglechatAlertProvider1 {
  'webhook-url': string;
  client?: ClientConfig5;
  'default-alert'?: Alert10;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: GooglechatOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Gotify
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "GotifyAlertProvider".
 */
export interface GotifyAlertProvider1 {
  /**
   * URL of the Gotify server
   */
  'server-url': string;
  /**
   * Token to use when sending a message to the Gotify server
   */
  token: string;
  /**
   * Priority of the message. Defaults to DefaultPriority.
   */
  priority?: number;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert11;
}
/**
 * Config used to check if the external endpoint has received new results when it should have.
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "HeartbeatConfig".
 */
export interface HeartbeatConfig1 {
  /**
   * Interval is the time interval at which Gatus verifies whether the external endpoint has received new results
   * If no new result is received within the interval, the endpoint is marked as failed and alerts are triggered
   */
  interval: string;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using HomeAssistant
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "HomeassistantAlertProvider".
 */
export interface HomeassistantAlertProvider1 {
  url: string;
  token: string;
  'default-alert'?: Alert12;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: HomeassistantOverride[];
}
/**
 * IAPConfig is the configuration for the Google Cloud Identity-Aware-Proxy
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "IAPConfig".
 */
export interface IAPConfig1 {
  /**
   * e.g. "toto.apps.googleusercontent.com"
   */
  audience: string;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using IFTTT
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "IftttAlertProvider".
 */
export interface IftttAlertProvider1 {
  /**
   * IFTTT Webhook key
   */
  'webhook-key': string;
  /**
   * IFTTT event name
   */
  'event-name': string;
  'default-alert'?: Alert13;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: IftttOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using ilert
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "IlertAlertProvider".
 */
export interface IlertAlertProvider1 {
  'integration-key': string;
  'default-alert'?: Alert14;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: IlertOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using incident.io
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "IncidentioAlertProvider".
 */
export interface IncidentioAlertProvider1 {
  url?: string;
  'auth-token'?: string;
  'source-url'?: string;
  metadata?: {
    [k: string]: unknown;
  };
  'default-alert'?: Alert15;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: IncidentioOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Line
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "LineAlertProvider".
 */
export interface LineAlertProvider1 {
  /**
   * Line Messaging API channel access token
   */
  'channel-access-token': string;
  /**
   * List of Line user IDs to send messages to
   */
  'user-ids': string[];
  'default-alert'?: Alert16;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: LineOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Matrix
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "MatrixAlertProvider".
 */
export interface MatrixAlertProvider1 {
  /**
   * ServerURL is the custom homeserver to use (optional)
   */
  'server-url': string;
  /**
   * AccessToken is the bot user's access token to send messages
   */
  'access-token': string;
  /**
   * InternalRoomID is the room that the bot user has permissions to send messages to
   */
  'internal-room-id': string;
  'default-alert'?: Alert17;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: MatrixOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Mattermost
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "MattermostAlertProvider".
 */
export interface MattermostAlertProvider1 {
  'webhook-url': string;
  channel?: string;
  client?: ClientConfig5;
  'default-alert'?: Alert18;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: MattermostOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Messagebird
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "MessagebirdAlertProvider".
 */
export interface MessagebirdAlertProvider1 {
  'access-key': string;
  originator: string;
  recipients: string;
  'default-alert'?: Alert19;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using n8n
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "N8nAlertProvider".
 */
export interface N8NAlertProvider1 {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert21;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: N8NOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using New Relic
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "NewrelicAlertProvider".
 */
export interface NewrelicAlertProvider1 {
  /**
   * New Relic Insert key
   */
  'insert-key': string;
  /**
   * New Relic account ID
   */
  'account-id': string;
  /**
   * Region (US or EU, defaults to US)
   */
  region?: string;
  'default-alert'?: Alert20;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: NewrelicOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Slack
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "NtfyAlertProvider".
 */
export interface NtfyAlertProvider1 {
  topic: string;
  /**
   * Defaults to DefaultURL
   */
  url?: string;
  /**
   * Defaults to DefaultPriority
   */
  priority?: number;
  /**
   * Defaults to ""
   */
  token?: string;
  /**
   * Defaults to ""
   */
  email?: string;
  /**
   * Defaults to ""
   */
  click?: string;
  /**
   * Defaults to false
   */
  'disable-firebase'?: boolean;
  /**
   * Defaults to false
   */
  'disable-cache'?: boolean;
  'default-alert'?: Alert22;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: NtfyOverride[];
}
/**
 * OAuth2Config is the configuration for the OAuth2 client credentials flow
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "OAuth2Config".
 */
export interface OAuth2Config1 {
  /**
   * e.g. https://dev-12345678.okta.com/token
   */
  'token-url': string;
  'client-id': string;
  'client-secret': string;
  /**
   * e.g. ["openid"]
   */
  scopes: string[];
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "OpsgenieAlertProvider".
 */
export interface OpsgenieAlertProvider1 {
  /**
   * APIKey to use for
   */
  'api-key': string;
  /**
   * Priority to be used in Opsgenie alert payload
   *
   * default: P1
   */
  priority: string;
  /**
   * Source define source to be used in Opsgenie alert payload
   *
   * default: gatus
   */
  source: string;
  /**
   * EntityPrefix is a prefix to be used in entity argument in Opsgenie alert payload
   *
   * default: gatus-
   */
  'entity-prefix': string;
  /**
   * AliasPrefix is a prefix to be used in alias argument in Opsgenie alert payload
   *
   * default: gatus-healthcheck-
   */
  'alias-prefix': string;
  /**
   * Tags to be used in Opsgenie alert payload
   *
   * default: []
   */
  tags: string[];
  'default-alert'?: Alert23;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using PagerDuty
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "PagerdutyAlertProvider".
 */
export interface PagerdutyAlertProvider1 {
  'integration-key': string;
  'default-alert'?: Alert24;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: PagerdutyOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Plivo
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "PlivoAlertProvider".
 */
export interface PlivoAlertProvider1 {
  'auth-id': string;
  'auth-token': string;
  from: string;
  to: string[];
  'default-alert'?: Alert25;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: PlivoOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Pushover
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "PushoverAlertProvider".
 */
export interface PushoverAlertProvider1 {
  /**
   * Key used to authenticate the application sending
   * See "Your Applications" on the dashboard, or add a new one: https://pushover.net/apps/build
   */
  'application-token': string;
  /**
   * Key of the user or group the messages should be sent to
   */
  'user-key': string;
  /**
   * The title of your message
   * default: "Gatus: <endpoint>""
   */
  title?: string;
  /**
   * Priority of all messages, ranging from -2 (very low) to 2 (Emergency)
   * default: 0
   */
  priority?: number;
  /**
   * Priority of resolved messages, ranging from -2 (very low) to 2 (Emergency)
   * default: 0
   */
  'resolved-priority'?: number;
  /**
   * Sound of the messages (see: https://pushover.net/api#sounds)
   * default: "" (pushover)
   */
  sound?: string;
  /**
   * TTL of your message (https://pushover.net/api#ttl)
   * If priority is 2 then this parameter is ignored
   * default: 0
   */
  ttl?: number;
  /**
   * Device to send the message to (see: https://pushover.net/api#devices)
   * default: "" (all devices)
   */
  device?: string;
  'default-alert'?: Alert26;
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "RemoteConfig".
 */
export interface RemoteConfig1 {
  /**
   * Instances is a list of remote instances to retrieve endpoint statuses from.
   */
  instances?: Instance[];
  client?: ClientConfig11;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Rocket.Chat
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "RocketchatAlertProvider".
 */
export interface RocketchatAlertProvider1 {
  /**
   * Rocket.Chat incoming webhook URL
   */
  'webhook-url': string;
  /**
   * Optional channel override
   */
  channel?: string;
  'default-alert'?: Alert27;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: RocketchatOverride[];
}
/**
 * Config is the security configuration for Gatus
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SecurityConfig".
 */
export interface SecurityConfig1 {
  basic?: BasicConfig;
  oidc?: OIDCConfig;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using SendGrid
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SendgridAlertProvider".
 */
export interface SendgridAlertProvider1 {
  'api-key': string;
  from: string;
  to: string;
  client?: ClientConfig6;
  'default-alert'?: Alert28;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SendgridOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Signal
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SignalAlertProvider".
 */
export interface SignalAlertProvider1 {
  /**
   * Signal API URL (e.g., signal-cli-rest-api instance)
   */
  'api-url': string;
  /**
   * Sender phone number
   */
  number: string;
  /**
   * List of recipient phone numbers
   */
  recipients: string[];
  'default-alert'?: Alert29;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SignalOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using SIGNL4
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "Signl4AlertProvider".
 */
export interface Signl4AlertProvider1 {
  /**
   * SIGNL4 team secret
   */
  'team-secret': string;
  'default-alert'?: Alert30;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: Signl4Override[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Slack
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SlackAlertProvider".
 */
export interface SlackAlertProvider1 {
  /**
   * Slack webhook URL
   */
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert31;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SlackOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Splunk
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SplunkAlertProvider".
 */
export interface SplunkAlertProvider1 {
  /**
   * Splunk HEC (HTTP Event Collector) URL
   */
  'hec-url': string;
  /**
   * Splunk HEC token
   */
  'hec-token': string;
  /**
   * Event source
   */
  source?: string;
  /**
   * Event source type
   */
  sourcetype?: string;
  /**
   * Splunk index
   */
  index?: string;
  'default-alert'?: Alert32;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SplunkOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Squadcast
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SquadcastAlertProvider".
 */
export interface SquadcastAlertProvider1 {
  /**
   * Squadcast webhook URL
   */
  'webhook-url': string;
  'default-alert'?: Alert33;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: SquadcastOverride[];
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "SshConfig".
 */
export interface SshConfig1 {
  username?: string;
  password?: string;
  'private-key'?: string;
}
/**
 * Config is the configuration for storage
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "StorageConfig".
 */
export interface StorageConfig1 {
  /**
   * Path is the path used by the store to achieve persistence
   * If blank, persistence is disabled.
   * Note that not all Type support persistence
   */
  path: string;
  /**
   * Type of store
   * If blank, uses the default in-memory store
   */
  type: string;
  /**
   * Caching is whether to enable caching.
   * This is used to drastically decrease read latency by pre-emptively caching writes
   * as they happen, also known as the write-through caching strategy.
   * Does not apply if Config.Type is not TypePostgres or TypeSQLite.
   */
  caching?: boolean;
  /**
   * MaximumNumberOfResults is the number of results each endpoint should be able to provide
   */
  'maximum-number-of-results'?: number;
  /**
   * MaximumNumberOfEvents is the number of events each endpoint should be able to provide
   */
  'maximum-number-of-events'?: number;
}
/**
 * TLSConfig is the configuration for mTLS configurations
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TLSConfig".
 */
export interface TLSConfig1 {
  /**
   * CertificateFile is the public certificate for TLS in PEM format.
   */
  'certificate-file'?: string;
  /**
   * PrivateKeyFile is the private key file for TLS in PEM format.
   */
  'private-key-file'?: string;
  renegotiation?: string;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Teams
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TeamsAlertProvider".
 */
export interface TeamsAlertProvider1 {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  client?: ClientConfig8;
  'default-alert'?: Alert34;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: TeamsOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Teams
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TeamsworkflowsAlertProvider".
 */
export interface TeamsworkflowsAlertProvider1 {
  'webhook-url': string;
  /**
   * Title of the message that will be sent
   */
  title?: string;
  'default-alert'?: Alert35;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: TeamsworkflowsOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Telegram
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TelegramAlertProvider".
 */
export interface TelegramAlertProvider1 {
  token: string;
  id: string;
  'topic-id'?: string;
  'api-url': string;
  client?: ClientConfig5;
  'default-alert'?: Alert36;
  /**
   * Overrides is a list of overrides that may be prioritized over the default configuration
   */
  overrides?: TelegramOverride[];
}
/**
 * Config represents the tunneling configuration
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TunnelingConfig".
 */
export interface TunnelingConfig1 {}
/**
 * AlertProvider is the configuration necessary for sending an alert using Twilio
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "TwilioAlertProvider".
 */
export interface TwilioAlertProvider1 {
  sid: string;
  token: string;
  from: string;
  to: string;
  /**
   * TODO in v6.0.0: Rename this to text-triggered
   */
  'text-twilio-triggered'?: string;
  /**
   * TODO in v6.0.0: Rename this to text-resolved
   */
  'text-twilio-resolved'?: string;
  'default-alert'?: Alert37;
}
/**
 * Config is the UI configuration for endpoint.Endpoint
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "UiConfig".
 */
export interface UiConfig2 {
  /**
   * HideConditions whether to hide the condition results on the UI
   */
  'hide-conditions': boolean;
  /**
   * HideHostname whether to hide the hostname in the Result
   */
  'hide-hostname': boolean;
  /**
   * HideURL whether to ensure the URL is not displayed in the results. Useful if the URL contains a token.
   */
  'hide-url': boolean;
  /**
   * HidePort whether to hide the port in the Result
   */
  'hide-port': boolean;
  /**
   * HideErrors whether to hide the errors in the Result
   */
  'hide-errors': boolean;
  /**
   * DontResolveFailedConditions whether to resolve failed conditions in the Result for display in the UI
   */
  'dont-resolve-failed-conditions': boolean;
  /**
   * ResolveSuccessfulConditions whether to resolve successful conditions in the Result for display in the UI
   */
  'resolve-successful-conditions': boolean;
  badge: Badge1;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Vonage
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "VonageAlertProvider".
 */
export interface VonageAlertProvider1 {
  'api-key': string;
  'api-secret': string;
  from: string;
  to: string[];
  'default-alert'?: Alert38;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: VonageOverride[];
}
/**
 * Config is the structure which supports the configuration of the server listening to requests
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "WebConfig".
 */
export interface WebConfig1 {
  /**
   * Address to listen on (defaults to 0.0.0.0 specified by DefaultAddress)
   */
  address: string;
  /**
   * Port to listen on (default to 8080 specified by DefaultPort)
   */
  port: number;
  /**
   * ReadBufferSize sets fiber.Config.ReadBufferSize, which is the buffer size for reading requests coming from a
   * single connection and also acts as a limit for the maximum header size.
   *
   * If you're getting occasional "Request Header Fields Too Large", you may want to try increasing this value.
   *
   * Defaults to DefaultReadBufferSize
   */
  'read-buffer-size'?: number;
  tls?: WebTLSConfig;
}
/**
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "WebTLSConfig".
 */
export interface WebTLSConfig1 {
  /**
   * CertificateFile is the public certificate for TLS in PEM format.
   */
  'certificate-file'?: string;
  /**
   * PrivateKeyFile is the private key file for TLS in PEM format.
   */
  'private-key-file'?: string;
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Webex
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "WebexAlertProvider".
 */
export interface WebexAlertProvider1 {
  /**
   * Webex Teams webhook URL
   */
  'webhook-url': string;
  'default-alert'?: Alert39;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: WebexOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Zapier
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ZapierAlertProvider".
 */
export interface ZapierAlertProvider1 {
  /**
   * Zapier webhook URL
   */
  'webhook-url': string;
  'default-alert'?: Alert40;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: ZapierOverride[];
}
/**
 * AlertProvider is the configuration necessary for sending an alert using Zulip
 *
 * This interface was referenced by `HttpsGithubComTwiNGatusV5ConfigConfig`'s JSON-Schema
 * via the `definition` "ZulipAlertProvider".
 */
export interface ZulipAlertProvider1 {
  /**
   * Email of the bot user
   */
  'bot-email': string;
  /**
   * API key of the bot user
   */
  'bot-api-key': string;
  /**
   * Domain of the Zulip server
   */
  domain: string;
  /**
   * ID of the channel to send the message to
   */
  'channel-id': string;
  'default-alert'?: Alert41;
  /**
   * Overrides is a list of Override that may be prioritized over the default configuration
   */
  overrides?: ZulipOverride[];
}
